# newnote
A simple command line tool to create a new subdirectory in my ~/notes/ directory with the passed in name of the note. The subdirectory is prefixed with a number to keep it naturally ordered within /notes/. A simple text file named notes.txt is copied into the new subdirectory.

Usage:
[python] newnote "Name_of_New_Subdirectory"
